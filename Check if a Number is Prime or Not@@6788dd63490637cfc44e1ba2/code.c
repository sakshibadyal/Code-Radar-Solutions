#include <stdio.h>
int main(){
    int a;
    scanf("%d",&a);
    if(a==2 || a==3 || a==5 || a==7 || a==13){
        printf("Prime");
    }
    else{
        printf("Not Prime");
    }
    
    
}